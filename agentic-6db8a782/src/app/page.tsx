import { AgentInterface } from "@/components/agent-interface";

export default function Home() {
  return (
    <main className="min-h-screen w-full bg-gradient-to-br from-zinc-50 via-white to-zinc-100 font-sans text-zinc-900">
      <AgentInterface />
    </main>
  );
}
